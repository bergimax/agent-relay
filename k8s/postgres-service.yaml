apiVersion: v1
kind: Service
metadata:
  name: postgres # DNS name the app uses: postgres:5432, mirroring compose.yaml
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
